Run the project test suite and report results.

1. Run `pytest` from the project root
2. If any tests fail, analyze the failures and suggest fixes
3. Report a summary: total tests, passed, failed, errors
4. If all tests pass, confirm with a brief summary
